import React from 'react';
import { Text } from 'react-native';
import { BottomTabSafeArea } from '../../components/BottomTabSafeArea';

export const Publish = (): JSX.Element => {
  return (
    <BottomTabSafeArea>
      <Text> Publish Works </Text>
    </BottomTabSafeArea>
  );
}
